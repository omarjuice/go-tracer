package main

func main() {
	// p := &Projectile{Point(0, .5, 0), Vector(1, 1, 0).Normalize()}

	// e := &Environment{Vector(0, -0.1, 0), Vector(-0.01, 0, 0)}
	// i := 0
	// for p.position[Y] >= 0 {
	// 	p = Tick(e, p)
	// 	i++
	// }
	// fmt.Println(i)
}
